name: PR branch check

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1

    - uses: actions/setup-node@v1
      with:
        node-version: '11.x'

    - name: Cache node modules
      uses: actions/cache@v1
      with:
        path: ~/.npm # npm cache files are stored in `~/.npm` on Linux/macOS
        key: ${{ runner.os }}-npm-$GITHUB_REPOSITORY-${{ env.branch-name }}-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
            ${{ runner.os }}-npm-$GITHUB_REPOSITORY-${{ env.branch-name }}-
            ${{ runner.os }}-npm-$GITHUB_REPOSITORY

    - name: Install Dependencies
      run: npm ci
                
    - name: Build
      run: npm run build